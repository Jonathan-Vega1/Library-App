<div class="jumbotron text-center">
  <h1>Contact</h1>
  <div class="form-horizontal form-group form-group-lg row">
    <!-- Email Field -->
    <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2">
      {{input type="email" value=emailAddress class="form-control" placeholder="Please type your e-mail address." autofocus="autofocus"}}
    </div>
    <!-- Text Field -->
    <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2">
      {{input type="text" value=textMessage class="form-control" placeholder="Please type your message." autofocus="autofocus"}}
    </div>
    <!-- Button -->
    <div class="col-xs-10 col-xs-offset-1 col-sm-offset-0 col-sm-4 col-md-3">
      <button class="btn btn-primary btn-lg btn-block" {{action 'sendMessage'}} disabled= {{isDisabled2}}>Send Message</button>
    </div>

  </div>

  {{#if contactResponse}}
    <div class="alert alert-success">{{contactResponse}}</div>
  {{/if}}

  <br/><br/>

</div>
